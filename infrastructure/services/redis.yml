services:
  redis:
    image: redis:alpine3.19
    container_name: redis
    restart: on-failure:5
    ports:
      - "6379:6379"
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
